# 1.0.24 validation

Completed locally: 64 regression checks, Linux Vulkan light/film final renders, Windows native library execution under Wine, Windows/Mac binary and export checks, and Mac ad-hoc signature page verification.

Required on target hardware before calling Windows/macOS production-validated: install matching ZIP, run RetroMan GPU Self-Test, check Rendered viewport, F12, cancellation, colored lights, transparent PNG output, and representative large scenes. Metal and Windows GPU execution are not verified by the Linux or Wine checks.
